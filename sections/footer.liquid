<footer class="footer full-width">
  <div class="footer__inner">
    <div class="footer__top">
      <a class="footer__brand" href="{{ routes.root_url }}">
        {% if section.settings.logo != blank %}
          {{
            section.settings.logo
            | image_url: width: 400
            | image_tag:
              class: 'footer__logo',
              alt: shop.name,
              widths: '120, 180, 240, 320, 400',
              sizes: '(max-width: 899px) 140px, 176px'
          }}
        {% else %}
          <img
            class="footer__logo"
            src="{{ 'kova-logo.png' | asset_url }}"
            alt="{{ shop.name | escape }}"
            width="220"
            height="84"
          >
        {% endif %}
      </a>

      <nav class="footer__nav" aria-label="{{ 'accessibility.footer_navigation' | t }}">
        {% if section.settings.menu != blank and section.settings.menu.links.size > 0 %}
          {% for link in section.settings.menu.links %}
            <a class="footer__link" href="{{ link.url }}">{{ link.title | escape }}</a>
          {% endfor %}
        {% else %}
          {% if section.settings.link_1_label != blank %}
            <a class="footer__link" href="{{ section.settings.link_1_url | default: '/collections/drop-001' }}">
              {{ section.settings.link_1_label | escape }}
            </a>
          {% endif %}
          {% if section.settings.link_2_label != blank %}
            <a class="footer__link" href="{{ section.settings.link_2_url | default: '/pages/build' }}">
              {{ section.settings.link_2_label | escape }}
            </a>
          {% endif %}
          {% if section.settings.link_3_label != blank %}
            <a class="footer__link" href="{{ section.settings.link_3_url | default: '/pages/about' }}">
              {{ section.settings.link_3_label | escape }}
            </a>
          {% endif %}
          {% if section.settings.link_4_label != blank %}
            <a class="footer__link" href="{{ section.settings.link_4_url | default: '/pages/manifesto' }}">
              {{ section.settings.link_4_label | escape }}
            </a>
          {% endif %}
          {% if section.settings.link_5_label != blank %}
            <a class="footer__link" href="{{ section.settings.link_5_url | default: '/pages/faq' }}">
              {{ section.settings.link_5_label | escape }}
            </a>
          {% endif %}
        {% endif %}

        {% if section.settings.instagram_url != blank %}
          <a
            class="footer__link"
            href="{{ section.settings.instagram_url }}"
            target="_blank"
            rel="noopener noreferrer"
          >
            {{ section.settings.instagram_label | escape }}
          </a>
        {% elsif section.settings.instagram_label != blank %}
          <span class="footer__link footer__link--muted">{{ section.settings.instagram_label | escape }}</span>
        {% endif %}
      </nav>
    </div>

    <p class="footer__copyright">
      &copy; {{ 'now' | date: '%Y' }}
      {{ section.settings.copyright_text | default: shop.name | escape }}
    </p>
  </div>
</footer>

{% stylesheet %}
  .footer {
    border-top: 1px solid var(--color-border);
    padding-block: clamp(2.25rem, 5vw, 3.5rem) clamp(1.5rem, 3vw, 2.25rem);
  }

  .footer__inner {
    display: grid;
    gap: clamp(2rem, 4vw, 3rem);
    width: 100%;
    max-width: var(--page-width);
    margin-inline: auto;
    padding-inline: var(--page-margin);
  }

  .footer__top {
    display: grid;
    gap: clamp(1.75rem, 4vw, 3rem);
    padding-bottom: clamp(1.75rem, 4vw, 2.5rem);
    border-bottom: 1px solid var(--color-border);
  }

  .footer__brand {
    justify-self: start;
    display: inline-flex;
    align-items: center;
    min-width: 0;
    min-height: 2.75rem;
    text-decoration: none;
  }

  .footer__logo {
    display: block;
    width: auto;
    height: 2.25rem;
    max-width: min(10.5rem, 70vw);
    object-fit: contain;
  }

  .footer__nav {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem 2rem;
  }

  .footer__link {
    position: relative;
    display: inline-flex;
    align-items: center;
    min-height: 2.5rem;
    color: var(--color-foreground-muted);
    text-decoration: none;
    font-family: var(--font-mono--family);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    transition: color var(--duration) var(--ease-out);
  }

  a.footer__link:hover,
  a.footer__link:focus-visible {
    color: var(--color-foreground);
  }

  .footer__link--muted {
    color: var(--color-foreground-muted);
  }

  .footer__copyright {
    color: var(--color-foreground-muted);
    font-family: var(--font-mono--family);
    font-size: var(--text-2xs);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
  }

  @media (max-width: 899px) {
    .footer__logo {
      height: 1.85rem;
      max-width: min(8.5rem, 64vw);
    }
  }

  @media (min-width: 900px) {
    .footer__top {
      grid-template-columns: auto 1fr;
      align-items: center;
      gap: 3rem;
    }

    .footer__nav {
      justify-content: flex-end;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footer__link {
      transition: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.footer",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "t:labels.logo"
    },
    {
      "type": "text",
      "id": "brand_label",
      "label": "t:labels.wordmark",
      "default": "KOVA"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:labels.menu",
      "info": "t:info.footer_menu"
    },
    {
      "type": "header",
      "content": "t:general.fallback_navigation"
    },
    {
      "type": "text",
      "id": "link_1_label",
      "label": "t:labels.link_1_label",
      "default": "DROP"
    },
    {
      "type": "url",
      "id": "link_1_url",
      "label": "t:labels.link_1_url"
    },
    {
      "type": "text",
      "id": "link_2_label",
      "label": "t:labels.link_2_label",
      "default": "BUILD"
    },
    {
      "type": "url",
      "id": "link_2_url",
      "label": "t:labels.link_2_url"
    },
    {
      "type": "text",
      "id": "link_3_label",
      "label": "t:labels.link_3_label",
      "default": "ABOUT"
    },
    {
      "type": "url",
      "id": "link_3_url",
      "label": "t:labels.link_3_url"
    },
    {
      "type": "text",
      "id": "link_4_label",
      "label": "t:labels.link_4_label",
      "default": "MANIFESTO"
    },
    {
      "type": "url",
      "id": "link_4_url",
      "label": "t:labels.link_4_url"
    },
    {
      "type": "text",
      "id": "link_5_label",
      "label": "t:labels.link_5_label",
      "default": "FAQ"
    },
    {
      "type": "url",
      "id": "link_5_url",
      "label": "t:labels.link_5_url"
    },
    {
      "type": "text",
      "id": "instagram_label",
      "label": "t:labels.instagram_label",
      "default": "INSTAGRAM"
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "t:labels.instagram_url"
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "t:labels.copyright_text",
      "default": "KOVA"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "t:labels.show_payment_icons",
      "default": false
    }
  ]
}
{% endschema %}
